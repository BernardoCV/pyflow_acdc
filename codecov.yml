coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto

ignore:
  - "pyflow_tests/**"
  - "pyflow_acdc/example_grids/**"
